import React from 'react'

const UserDetail = ({ user }) => {
  console.log(user);
  return (
    <div>
      {/* <ul className="users__total-ul">
        <li>{user.name}</li>
        <li>{user.email}</li>
        <li>{user.id}</li>
        <li>{user.detail}</li>
        <hr></hr>
      </ul> */}
    </div>
  )
}

export default UserDetail
